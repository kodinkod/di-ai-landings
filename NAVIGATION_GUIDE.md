# Руководство по навигации

## Что было реализовано

### ✨ Система вкладок

Лендинг теперь имеет две вкладки:

1. **Главная** - основной контент лендинга
2. **Библиотека промптов** - отдельная страница с 40+ промптами

### 🎯 Компоненты

#### Navigation.jsx
Верхняя навигационная панель с:
- Логотипом (🤖 AI Ассистент)
- Двумя вкладками (Главная / Библиотека промптов)
- Бейджем "40+" на вкладке промптов
- Кнопкой "Открыть бота" (скрыта на мобильных)

**Особенности:**
- Sticky navigation (прилипает к верху при скролле)
- Стеклянный эффект (backdrop-blur)
- Активная вкладка выделена градиентом
- Адаптивная (скрывает текст на мобильных)

#### App.jsx
Управление состоянием:
```jsx
const [currentTab, setCurrentTab] = useState('home')
```

**Логика переключения:**
- Клик на вкладку → смена контента
- Автоматический скролл наверх при переключении
- Условный рендеринг (home или prompts)

## Структура вкладок

### Вкладка "Главная"
```
- Hero
- UseCases (4 кейса)
- NanaBananaShowcase (топ-3 промпта)
  └─ Кнопка "Смотреть все 40+ промптов"
- OCRShowcase
- Features
- CTA
- Footer
```

### Вкладка "Библиотека промптов"
```
- PromptsLibrary (40+ промптов)
  ├─ Популярные промпты
  ├─ Поиск
  ├─ Категории
  └─ Все промпты
- Footer
```

## Пользовательский поток

### Вариант 1 (через кнопку):
1. Пользователь на главной
2. Видит топ-3 промпта
3. Нажимает "Смотреть все 40+ промптов"
4. → Переключается на вкладку "Библиотека промптов"
5. Скролл автоматически наверх

### Вариант 2 (через навигацию):
1. Пользователь кликает на вкладку "Библиотека промптов"
2. → Переключается на библиотеку
3. Скролл наверх

### Возврат на главную:
- Клик на вкладку "Главная" в навигации
- Скролл наверх

## Технические детали

### Состояние
```jsx
const [currentTab, setCurrentTab] = useState('home')
// 'home' или 'prompts'
```

### Переключение с автоскроллом
```jsx
const handleTabChange = (tab) => {
  setCurrentTab(tab)
  window.scrollTo({ top: 0, behavior: 'smooth' })
}
```

### Передача функции в компонент
```jsx
<NanaBananaShowcase
  onViewAllPrompts={() => handleTabChange('prompts')}
/>
```

## Адаптивность

### Desktop:
- Обе вкладки с полным текстом
- Кнопка "Открыть бота" видна

### Tablet:
- Вкладки с полным текстом
- Кнопка "Открыть бота" скрыта

### Mobile:
- "Главная" вместо "Главная"
- "Промпты" вместо "Библиотека промптов"
- Бейдж "40+" сохраняется

## Стили навигации

### Sticky navigation
```jsx
className="sticky top-0 z-40"
```

### Стеклянный эффект
```jsx
className="bg-white/80 backdrop-blur-md"
```

### Активная вкладка
```jsx
currentTab === 'home'
  ? 'bg-gradient-to-r from-blue-600 to-purple-600 text-white'
  : 'text-gray-700 hover:bg-gray-100'
```

## Преимущества подхода

✅ **Чистота главной страницы:**
- Библиотека не занимает место
- Фокус на основном контенте
- Быстрая загрузка

✅ **Удобная навигация:**
- Всегда доступна наверху
- Понятно где находишься
- Легко переключаться

✅ **SEO дружественно:**
- Весь контент остается доступным
- Нет динамической загрузки
- Быстрая навигация

✅ **Производительность:**
- Рендерится только текущая вкладка
- Меньше DOM элементов
- Быстрее работает

## Кастомизация

### Добавить третью вкладку

1. В Navigation.jsx добавить кнопку:
```jsx
<button
  onClick={() => onTabChange('new-tab')}
  className={...}
>
  Новая вкладка
</button>
```

2. В App.jsx добавить условие:
```jsx
{currentTab === 'home' ? (
  // Главная
) : currentTab === 'prompts' ? (
  // Промпты
) : (
  // Новая вкладка
  <NewTabContent />
)}
```

### Изменить цвета навигации

В Navigation.jsx:
```jsx
// Фон
bg-white/80 → bg-gray-900/90

// Активная вкладка
from-blue-600 to-purple-600 → from-green-600 to-blue-600

// Бейдж
bg-yellow-400 → bg-red-500
```

### Добавить подменю

Можно добавить выпадающее меню при наведении на вкладку.

## URL синхронизация (опционально)

Если нужно чтобы вкладки имели свои URL:

### Установить React Router:
```bash
npm install react-router-dom
```

### Изменить App.jsx:
```jsx
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom'

function App() {
  return (
    <BrowserRouter>
      <Navigation />
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/prompts" element={<PromptsPage />} />
      </Routes>
    </BrowserRouter>
  )
}
```

Но для лендинга это избыточно - текущий вариант проще и легче.

## Замена URL бота

В Navigation.jsx найдите:
```jsx
window.open('https://t.me/YOUR_BOT_USERNAME', '_blank')
```

Замените на ваш бот.

## Итого

Теперь лендинг имеет:
- ✅ Чистую навигацию с двумя вкладками
- ✅ Главную страницу с основным контентом
- ✅ Отдельную страницу библиотеки промптов
- ✅ Плавные переходы между вкладками
- ✅ Автоскролл наверх при переключении

**Библиотека больше не занимает место на главной! 🎉**
