# Гайд по промптам для Нана Банана

## Что изменилось

Секция "Нана Банана" теперь показывает **готовые промпты**, которые пользователи могут использовать сами.

При нажатии на кнопку "Попробовать промпты" открывается модальное окно с:
- Инструкциями по использованию
- Списком готовых промптов
- Кнопкой копирования промпта
- Ссылкой на бота

## Функционал

### 1. Карточки с примерами промптов

Каждая карточка показывает:
- **Основной промпт** (в черном баннере на изображении)
- **2 варианта промптов** в предпросмотре
- **Кнопка "Попробовать промпты"** - открывает модальное окно

### 2. Модальное окно

Когда пользователь нажимает "Попробовать промпты":

**Показывается:**
- Пошаговая инструкция (1-2-3)
- Полный список всех промптов для этого кейса
- Кнопка копирования каждого промпта
- Совет о том, что промпты можно изменять
- Кнопка "Открыть бота и попробовать"

**Интерактивность:**
- Клик по кнопке копирования → промпт в буфер обмена
- Показ галочки при успешном копировании
- Клик вне модального окна → закрытие

### 3. Широкие карточки

Сетка изменена с 3 колонок на **2 колонки** для показа ДО/ПОСЛЕ изображений.

## Добавление своих промптов

### В файле `src/components/NanaBananaShowcase.jsx`

Найдите массив `showcaseCases` и измените поля:

```jsx
{
  id: 'styling',
  icon: Palette,
  title: 'Название кейса',
  description: 'Краткое описание',
  prompt: 'основной промпт',  // Показывается на карточке
  promptVariants: [
    'первый вариант промпта',
    'второй вариант промпта',
    'третий вариант промпта',
    // можно добавить больше
  ],
  imagePlaceholder: 'filename.jpg',
  instructions: [
    'Шаг 1',
    'Шаг 2',
    'Шаг 3',
  ],
}
```

## Примеры промптов по кейсам

### Стилизация фото
- `переделай фото в стиле студии Гибли`
- `сделай фото в стиле Disney`
- `преобразуй в аниме стиль`
- `сделай в стиле Pixar`
- `преврати в акварельную живопись`

### Замена погоды
- `сделай на фото солнечную погоду`
- `добавь дождь на фото`
- `преврати в снежный день`
- `добавь туман и облака`
- `сделай закатное освещение`

### Фото на документы
- `сделай фото на документы`
- `фото на паспорт`
- `фото на визу США`
- `фото на водительские права`

### Ретушь фото
- `сделай ретушь лица`
- `улучши кожу на фото`
- `профессиональная ретушь портрета`
- `убери недостатки кожи`
- `улучши освещение на лице`

### Примерка одежды
- `переодень в деловой костюм`
- `добавь кожаную куртку`
- `одень в вечернее платье`
- `переодень в спортивную форму`
- `добавь джинсовую куртку и джинсы`

## Советы для пользователей

Можно добавить в модальное окно дополнительные советы:

1. **Будьте конкретны**: "переодень в красное платье" лучше чем "переодень"
2. **Добавляйте детали**: "сделай солнечный закат с оранжевым небом"
3. **Экспериментируйте**: Комбинируйте разные стили
4. **Используйте русский язык**: AI понимает естественный язык

## Изменение текстов модального окна

### Заголовок секции
Строка 140 в `NanaBananaShowcase.jsx`:
```jsx
Преобразите фото с помощью <span className="gradient-text">промптов</span>
```

### Подзаголовок
Строка 143:
```jsx
Просто отправьте фото и промпт в бота - AI сделает всё сам
```

### Совет в модальном окне
Строка 363:
```jsx
💡 Совет: Вы можете изменять промпты под свои нужды - добавляйте детали, цвета, стили!
```

## Кастомизация модального окна

### Цвета
Градиенты используют purple и pink:
```jsx
from-purple-600 to-pink-600
```

Можно изменить на свои цвета в Tailwind формате.

### Размер модального окна
Строка 285:
```jsx
max-w-2xl  // Можно изменить на max-w-3xl, max-w-4xl и т.д.
```

### Высота карточек
Строка 158:
```jsx
h-80  // Можно изменить на h-64, h-96 и т.д.
```

## Отключение модального окна

Если хотите чтобы кнопка сразу открывала бота:

Замените в строке 230:
```jsx
// Было:
onClick={() => handleTryPrompt(item)}

// Стало:
onClick={() => window.open('https://t.me/YOUR_BOT_USERNAME', '_blank')}
```

## Аналитика (опционально)

Можно добавить отслеживание кликов:

```jsx
const handleTryPrompt = (item) => {
  // Добавьте вашу аналитику
  // gtag('event', 'prompt_clicked', { case_id: item.id })

  setSelectedCase(item)
}

const handleCopyPrompt = (prompt) => {
  // gtag('event', 'prompt_copied', { prompt: prompt })

  navigator.clipboard.writeText(prompt)
  setCopiedPrompt(true)
  setTimeout(() => setCopiedPrompt(false), 2000)
}
```

## Доступность

Компонент включает:
- ✅ Keyboard navigation (Tab для навигации)
- ✅ Закрытие модального окна по Escape (можно добавить)
- ✅ ARIA labels для кнопок
- ✅ Фокус на модальном окне

Для добавления закрытия по Escape:

```jsx
useEffect(() => {
  const handleEscape = (e) => {
    if (e.key === 'Escape') closeModal()
  }

  if (selectedCase) {
    document.addEventListener('keydown', handleEscape)
    return () => document.removeEventListener('keydown', handleEscape)
  }
}, [selectedCase])
```
