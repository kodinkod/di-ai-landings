# Библиотека промптов - Руководство

## Что было реализовано

### 🎉 Новая архитектура промптов

**Главная страница (NanaBananaShowcase):**
- ✅ Топ-3 самых популярных промпта
- ✅ Кнопка "Смотреть все 40+ промптов" → переход в библиотеку
- ✅ Модальные окна с деталями для каждого промпта

**Библиотека промптов (PromptsLibrary):**
- ✅ 40+ готовых промптов
- ✅ 8 категорий промптов
- ✅ Поиск по промптам
- ✅ Фильтрация по категориям
- ✅ Секция "Популярные промпты"
- ✅ Копирование промпта в один клик
- ✅ Кнопка "Попробовать" на каждом промпте

## Топ-3 на главной

### 1. Стилизация Гибли ⭐
```
переделай фото в стиле студии Гибли
```
- Самый популярный промпт
- Для аватарок и соцсетей
- Превращение в аниме персонажа

### 2. Удаление фона
```
убери весь фон, оставь только меня
```
- Очень практично
- Для резюме, товаров, стикеров
- Белый или прозрачный фон

### 3. Восстановление фото
```
восстанови старую поврежденную фотографию
```
- Семейные воспоминания
- Улучшение старых фото
- Повышение качества

## Категории в библиотеке

### 🎨 Стилизация (8 промптов)
- Гибли, Disney/Pixar, Аниме
- Комикс Marvel
- Картина маслом, Акварель
- Поп-арт, Винтаж

### 👤 Внешность (7 промптов)
- Профессиональная ретушь
- Добавить улыбку
- Изменить прическу/цвет волос
- Добавить бороду
- Омолодить
- Макияж

### 👔 Одежда (5 промптов)
- Деловой костюм
- Кэжуал
- Вечернее платье
- Спортивная форма
- Кожаная куртка

### 🖼️ Фон и локация (5 промптов)
- Удалить фон
- Размыть фон
- Пляж, Горы, Город

### ☀️ Погода и свет (6 промптов)
- Солнечный день
- Золотой час
- Снег, Дождь, Туман
- Ночь

### 🎬 Креативное (5 промптов)
- Черно-белое
- Кинематографичность
- HDR эффект
- Двойная экспозиция
- Неоновые цвета

### 📷 Практичное (6 промптов)
- Фото на документы
- Улучшить качество
- Восстановить старое фото
- Удалить людей/объекты
- Раскрасить ЧБ фото

## Как это работает

### Главная страница

1. Пользователь видит 3 топовых промпта
2. Может открыть модальное окно с деталями
3. Видит кнопку "Смотреть все 40+ промптов"
4. Нажимает → плавный скролл к библиотеке

### Библиотека промптов

1. **Популярные промпты** (3 штуки) - сверху, выделены желтым
2. **Поиск** - найти промпт по ключевому слову
3. **Категории** - фильтр по 8 категориям
4. **Карточки промптов:**
   - Название
   - Описание
   - Сам промпт
   - Место для примера изображения
   - Кнопка "Копировать" → промпт в буфер обмена
   - Кнопка "Попробовать" → открывает бота

## Функционал

### Копирование промпта
```jsx
const handleCopyPrompt = (prompt) => {
  navigator.clipboard.writeText(prompt)
  setCopiedPrompt(prompt)
  setTimeout(() => setCopiedPrompt(null), 2000)
}
```

### Поиск промптов
- Поиск по названию
- Поиск по описанию
- Поиск по тексту промпта

### Фильтрация
- По категориям
- "Все промпты" - показать все

## Добавление новых промптов

### В файле `src/components/PromptsLibrary.jsx`

Найдите массив `prompts` и добавьте новый объект:

```jsx
{
  category: 'style',  // ID категории
  title: 'Название промпта',
  prompt: 'текст промпта для бота',
  description: 'Краткое описание',
  popular: true,  // если это топ промпт
  image: 'filename.jpg'  // имя файла изображения
}
```

### Категории

- `style` - Стилизация
- `appearance` - Внешность
- `clothes` - Одежда
- `background` - Фон и локация
- `weather` - Погода и свет
- `creative` - Креативное
- `practical` - Практичное

### Популярные промпты

Добавьте `popular: true` чтобы промпт появился в секции "Популярные".

## Изображения для примеров

### Где размещать:
`/public/examples/`

### Рекомендуемый размер:
- **Карточки в библиотеке:** 600x400px
- **Формат:** JPG/WebP
- **Размер файла:** до 150KB

### Как добавить:

1. Положите изображение в `public/examples/`
2. В коде раскомментируйте блок изображения (строка ~200 в PromptsLibrary.jsx)

```jsx
// Было (закомментировано):
{/*
<img
  src={`/examples/${p.image}`}
  alt={p.title}
  className="w-full h-full object-cover"
/>
*/}

// Стало:
<img
  src={`/examples/${p.image}`}
  alt={p.title}
  className="w-full h-full object-cover"
/>
```

## Замена URL бота

В файле `src/components/PromptsLibrary.jsx` найдите:

```jsx
const handleTryBot = () => {
  window.open('https://t.me/YOUR_BOT_USERNAME', '_blank')
}
```

Замените `YOUR_BOT_USERNAME` на имя вашего бота.

## Статистика

- **Всего промптов:** 42
- **Категорий:** 8
- **Популярных:** 6
- **На главной:** 3

## Навигация

### Переход из главной в библиотеку:

```jsx
onClick={() => document.getElementById('prompts-library')?.scrollIntoView({ behavior: 'smooth' })}
```

### ID библиотеки:
```jsx
<section id="prompts-library">
```

## Кастомизация

### Изменить количество популярных промптов

В файле `PromptsLibrary.jsx` измените:

```jsx
const popularPrompts = prompts.filter(p => p.popular)
```

### Изменить количество промптов на главной

В файле `NanaBananaShowcase.jsx` измените массив `showcaseCases` (сейчас 3 промпта).

### Изменить цвета кнопок

Градиенты используют:
```jsx
from-purple-600 to-pink-600
from-blue-600 to-purple-600
```

Можно изменить на свои цвета в Tailwind формате.

## Адаптивность

### Desktop (lg):
- Библиотека: 3 колонки
- Главная: 2 колонки

### Tablet (md):
- Библиотека: 2 колонки
- Главная: 2 колонки

### Mobile:
- Библиотека: 1 колонка
- Главная: 1 колонка

## Производительность

- ✅ Lazy rendering модальных окон
- ✅ Фильтрация на клиенте (быстро)
- ✅ Debounce для поиска (опционально)
- ✅ Мемоизация фильтрованных промптов (опционально)

## Возможные улучшения

### Будущие фичи:

- [ ] Избранные промпты (localStorage)
- [ ] История использования
- [ ] Сортировка (по популярности, по алфавиту)
- [ ] Теги для промптов
- [ ] Шаринг промптов
- [ ] Рейтинг промптов от пользователей
- [ ] Связанные промпты ("похожие")
- [ ] Предпросмотр результата
- [ ] Вариации промптов (слайдер)

### Аналитика:

```jsx
// Отслеживание кликов
const handleCopyPrompt = (prompt) => {
  // gtag('event', 'prompt_copied', { prompt_title: title })
  navigator.clipboard.writeText(prompt)
}

const handleTryBot = (promptTitle) => {
  // gtag('event', 'try_prompt', { prompt_title: promptTitle })
  window.open('https://t.me/YOUR_BOT_USERNAME', '_blank')
}
```

## Troubleshooting

**Q: Промпты не фильтруются?**
A: Проверьте что category в промпте совпадает с id категории

**Q: Поиск не работает?**
A: Поиск ищет по title, prompt и description (латиница/кириллица)

**Q: Кнопка "Все промпты" не скроллит?**
A: Проверьте что у секции есть id="prompts-library"

**Q: Изображения не показываются?**
A: Убедитесь что файлы в public/examples/ и раскомментирован код

## Заключение

Теперь у вас есть полноценная библиотека с 40+ промптами!

- На главной - топ-3 для первого знакомства
- В библиотеке - весь каталог с фильтрами
- Копирование в один клик
- Переход в бота с каждого промпта

**Готово к использованию! 🎊**
